#include<stdio.h>
int main()
{
   char str[50];
   int i;
   printf("Enter a string ");
   gets(str);
   for(i=0;str[i]!='\0';i++);

   printf("Reversed string: ");
   for(i=i-1;i>=0;i--)
   {
       printf("%c",str[i]);
   }
   return 0;

}
